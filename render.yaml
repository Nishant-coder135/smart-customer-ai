# Render.com deployment config
services:
  - type: web
    name: smartcustomer-ai
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: .
    plan: free
    envVars:
      - key: PORT
        value: 8000
      - key: PYTHONPATH
        value: /app
